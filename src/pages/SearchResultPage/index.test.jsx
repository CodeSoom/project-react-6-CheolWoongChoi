import { render } from '@testing-library/react';

import SearchResultPage from '.';

describe('SearchResultPage', () => {
  const renderSearchResultPage = () => render(<SearchResultPage />);

  it('', () => {

  });
});
